# Android client

This folder contains a minimal Android Compose app that calls the local FastAPI recommendation endpoint.

## Run

Open the android folder in Android Studio and run the app on an emulator or device.

## Backend URL

The current app points to:

- http://127.0.0.1:8002/

If you run the backend on another port, update the baseUrl in MainActivity.kt.
